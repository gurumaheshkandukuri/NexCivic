import { createIssue } from "./src/services/issueService.js";

async function run() {
    console.log("Starting script...");
    // Just mock enough of the firebase stuff if needed... wait, I can just write a quick script that uses the compiled dist if it's there? No.
}
run();
